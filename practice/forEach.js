let array = [1,2,3,4,5,6]

// array.forEach((num, index) => (array[index] = num * 2))
// console.log(array)

const doubled = array.map((num) => num * 2)
console.log(doubled)
console.log(array)

// * reduce

